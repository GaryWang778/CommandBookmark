package com.command;

//客户端
public class Client {
    public static void main(String[] args) {
        commandManager manager = new commandManager();
        //manager.executeCommand(new addCommand());
        //manager.undo();
        //manager.redo();
    }
}